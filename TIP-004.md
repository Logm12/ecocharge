# Vibecode Kit v6.0 — Task Instruction Pack

## ROLE
You are the BUILDER in Vibecode Kit v6.0. 
The Contractor (Claude Chat) has reviewed TIP-003 and is highly impressed with the Framer Motion implementation. We are now in the final UI build phase.

## ABSOLUTE RULES
1. IMPLEMENT exactly per TIP specification below.
2. DO NOT change architecture / structure.
3. DO NOT add features outside TIP.
4. SELF-TEST per acceptance criteria.
5. REPORT per Completion Report format.

## PROJECT CONTEXT
- **Project**: EcoCharge Landing Page.
- **Tech Stack**: Next.js, Tailwind CSS v4.
- **Remaining Requirements**: REQ-004 (Impact/ESG Metrics), REQ-005 (Team Section).

---

# TIP-004: Completion — Impact, Team & Final Assembly

## HEADER
- TIP-ID: TIP-004
- Project: EcoCharge Landing Page
- Module: Completion & Polish
- Depends on: TIP-001, TIP-002, TIP-003
- Priority: P0
- Estimated effort: 45 minutes

## CONTEXT
- Working directory: `src/components/sections/`
- Key files to create/modify: `ImpactSection.tsx`, `TeamSection.tsx`, `src/app/page.tsx`, `src/components/ui/Card.tsx` (optional).

## TASK
1. Create `ImpactSection.tsx` focusing on Net Zero and ESG metrics.
2. Create `TeamSection.tsx` to introduce the VNU (ĐHQGHN) founders.
3. Assemble the final `page.tsx` layout: Hero → Problem → Tech (Scroll) → Impact → Team.
4. Perform a Tier 1 basic QA self-test (check for console errors, broken layouts).

## SPECIFICATIONS

**1. Impact Section (`ImpactSection.tsx`):**
- Purpose: Showcase the massive environmental benefit.
- Styling: Use `bg-house-green` (`#1E3932`) with `text-canvas` (`#f2f0eb`) to make the numbers pop. 
- Core Metrics to highlight boldly: 
  - **60,000 kWh/year** (Energy generated per system)
  - **-60 Tons CO2** (Carbon footprint reduction)
- Layout: Use a grid or flex layout for the stats. Add an ESG/Net Zero statement.

**2. Team Section (`TeamSection.tsx`):**
- Purpose: Build trust with the international council.
- Styling: Return to `bg-canvas` or `bg-surface`. Use simple `shape-card` (12px rounded corners) for team members.
- Content: "Founding Team from VNU (Vietnam National University, Hanoi)". You don't need real photos yet, use generic avatar placeholders or Lucide icons for now, but lay out the grid for the 5 founders (CEO, COO, CMO, etc.). Include a final "Contact Us / Partner with Us" CTA button at the very bottom.

**3. Final Assembly (`page.tsx`):**
- Ensure spacing between sections is consistent (e.g., `py-20` or `py-24`).
- The flow must be seamless from top to bottom.

## ACCEPTANCE CRITERIA
Given: The `ImpactSection`
When: Rendered on the screen
Then: The metrics "60,000 kWh" and "60 Tons CO2" must be the largest, most visible text elements in that section.

Given: The `TeamSection`
When: Rendered on the screen
Then: It must display a layout accommodating the founding team and clearly mention "VNU".

Given: The fully assembled `page.tsx`
When: Running `npm run dev` and scrolling from top to bottom
Then: There must be zero React hydration errors, zero console warnings, and the visual flow must match the Starbucks-inspired Design System without any broken responsive breakpoints on Desktop.

## CONSTRAINTS
- Stick to the pre-defined tokens (`accent-green`, `house-green`, `canvas`, `surface`).
- Keep the code clean. Remove any unused imports.

## AFTER COMPLETION
Create COMPLETION REPORT per format below. Note: After this TIP is accepted, we will enter the VERIFY phase.

```text
## COMPLETION REPORT — TIP-004

**STATUS:** DONE / PARTIAL / BLOCKED

**FILES CHANGED:**
- Created: [list + purpose]
- Modified: [list + change description]

**TEST RESULTS:**
- Acceptance criteria tested: [X/Y passed]
- Details: [pass/fail per criteria]

**ISSUES DISCOVERED:**
- [Issue]: [severity] — [description] — [suggestion]

**DEVIATIONS FROM SPEC:**
- [Deviation]: [what] — [why] — [impact]

**SUGGESTIONS FOR CHỦ THẦU:**
- [Suggestion]: [observation] — [recommendation]